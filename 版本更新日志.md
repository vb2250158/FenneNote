# 版本更新日志

## 最终归档版 - 2026-07-17

- FenneNote 结束维护；麦克风常驻监听、静音切段、本地 ASR 和 Route 提交迁移到 RabiRoute 的 RabiPC「语音消息端」与 RabiSpeech。
- 配置升级到版本 6，删除 DashScope/MiMo API、云端说话人分离和 API key 字段。
- GUI 只允许选择本地模型；读取到旧 `model_source: api` 时明确报错，不会发起网络请求。
- 配置说明改为迁移指南，并增加云端实现的 Git 历史归档索引。
- 保留本地 faster-whisper 兼容路径，便于读取旧配置和数据，但不再建议把 FenneNote 作为常驻运行入口。

模型缓存、录音、转写文本、token、日志和个人配置不属于公开归档内容。
